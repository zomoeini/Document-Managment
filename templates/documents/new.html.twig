{% extends 'base.html.twig' %}

{% block title %}New Documents{% endblock %}

{% block body %}
    <div class="container">
    <h1>Create new Documents</h1>

    {{ include('documents/_form.html.twig') }}

    <a class="btn btn-info" href="{{ path('documents_index') }}">back to list</a>
    </div>
{% endblock %}
